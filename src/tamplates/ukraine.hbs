{{#each this}}
<li class="section-item">
    <p class="category-black">{{category}}</p>
    <img id={{id}} class="section-image" src={{image}} alt="">
    <h2 class="title">{{title}}</h2>
    <p class="date">{{date}}</p>
</li>
{{/each}}